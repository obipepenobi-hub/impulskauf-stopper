org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# GitHub repo that hosts releases for the in-app update checker.
# Update this if the repo is forked/renamed.
UPDATE_REPO_OWNER=liam
UPDATE_REPO_NAME=impulskauf-stopper
